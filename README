Requirements:
openjdk 10.0.2 2018-07-17
Apache Maven 3.5.2
PostgreSQL 11.1

Dependencies install:
mvn install
Build:
mvn compile

Run (WARNING: root is required!)
mvn exec:java -Dexec.mainClass=bitworks.tz.App

This application may by work only the PostgreSQL database.

Database configuration and  limit date be located into config.ini file.